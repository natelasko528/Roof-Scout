import { Injectable } from '@angular/core';

/**
 * This service is no longer in use after switching from Google Maps to OpenStreetMap.
 * It is left empty to prevent build errors from any lingering imports.
 */
@Injectable({
    providedIn: 'root'
})
export class MapsLoaderService {}
